import 'package:flutter/material.dart';

import 'package:arocs_controller/models/flowin/flow_in_model.dart';

class ProgressPage extends StatelessWidget {
  const ProgressPage({super.key, required this.flowIn});
  final FlowInModel flowIn;

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Column(
        children: [
          
        ],
      ),
    );
  }
}